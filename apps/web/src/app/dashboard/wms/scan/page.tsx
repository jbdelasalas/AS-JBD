'use client';

import ScanMoveWorkbench from '@/components/ScanMoveWorkbench';

export default function WmsScanPage() {
  return <ScanMoveWorkbench subtitle="Scan a box or pallet, then scan the destination bin." />;
}
